#include <stdio.h>
int main(){
    int a,i;
    int factors = 0;
    if (n<=1){
        printf("Not Prime");
    }
    else{
        for (i=1;i<=a;i++){
            if (n%i==0)
                b++;
            if (b>2)
                printf("Not Prime");
            else    printf("Prime");
        }
    }
    return 0;
}